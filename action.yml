name: 'ArgoCD Application Updater'
description: 'Provide a description here'
author: 'hipages'
inputs:
  files:
    description: 'Files containing the application manifest'
    default: '.argocd**.y*ml'
    required: false
runs:
  using: 'node12'
  main: 'dist/index.js'
